# Obvolávání

Internal CRM module built with **Next.js (App Router)**, **TypeScript**, and **Tailwind CSS**.

## Development

```bash
npm run dev
```

App entry: `app/page.tsx` (currently renders `components/calling/CallingModuleLayout.tsx`).
